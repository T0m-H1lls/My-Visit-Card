            Пометки:
    <strong> — жирный (важность).
    <b> — просто жирный (без смысла).
    <em> — курсив (акцент).
    <i> — просто курсив.
    <mark> — жёлтая подсветка (выделение).
    <u> — подчёркивание.
    <del> — зачёркнутый (удалённый текст).
    <sub> — нижний индекс (H2O — вода).
    <sup> — верхний индекс (x²).


            3.5. Код и цитаты
    <code> — код в тексте (моноширинный шрифт).
    <pre> — «предварительно отформатированный» текст: сохраняет все пробелы и переносы.
    <blockquote> — длинная цитата-блок.
    <q> — короткая цитата внутри строки.

            6. Таблицы
    Таблицы строятся из строк и ячеек. Основные теги:

    <table> — сама таблица.
    <tr> — строка (table row).
    <th> — ячейка-заголовок (table header).
    <td> — обычная ячейка (table data).
    <caption> — подпись таблицы.
    <thead> — «шапка» таблицы (группа строк-заголовков).
    <tbody> — основное тело таблицы.
    <tfoot> — «подвал» таблицы.

                 Поле ввода <input>
    <input> — одиночный тег. Его тип задаётся атрибутом type.

    <input type="text" name="username" placeholder="Введите имя">

                Атрибуты:

        type — тип поля (ниже).
        name — имя поля, важное для отправки данных (данные уходят как пары «имя=значение»).
        placeholder — серый подсказочный текст внутри поля, исчезает при вводе.
        value — значение по умолчанию.

        Основные типы type:

        Тип     	Что делает
        text	    Обычное текстовое поле
        password	Поле для пароля (точки)
        email	    Проверяет, что введён e-mail
        number	    Поле для чисел (со стрелками)
        tel	        Поле для телефона
        date	    Выбор даты из календаря
        checkbox	Галочка (может быть отмечена)
        radio	    Переключатель (один из группы)
        file	    Выбор файла
        submit	    Кнопка отправки
        reset	    Кнопка сброса
        button	    Обычная кнопка


             9.2. Основные семантические теги
        Тег	             Роль
        <header>	    Шапка страницы или раздела
        <nav>	        Навигация (меню)
        <main>	        Основное содержимое страницы
        <section>	    Логический раздел страницы
        <article>	    Самостоятельная статья/запись
        <aside>	        Боковая колонка, дополнительная информация
        <footer>	    Подвал страницы
        <figure>	    Иллюстрация с подписью


    10.1. Аудио <audio>

    <audio controls>
        <source src="music.mp3" type="audio/mpeg">
        Ваш браузер не поддерживает аудио.
    </audio>
    Текст между тегами показывается, если браузер audio не поддерживает.


    Атрибуты audio:

    controls — показать панель управления (play, pause, громкость).
    autoplay — начать воспроизведение автоматически.
    loop — повторять в цикле.
    muted — выключить звук по умолчанию.



                10.2. Видео <video>
    <video controls width="640" height="360" poster="preview.jpg">
        <source src="movie.mp4" type="video/mp4">
        Ваш браузер не поддерживает видео.
    </video>

    Атрибуты video:

    controls — панель управления.
    poster — картинка-заставка до начала воспроизведения.
    width / height — размер.
    autoplay, loop, muted — аналогично аудио.
    ℹ️ Форматы: аудио обычно .mp3, .ogg, .wav; видео — .mp4, .webm, .ogg. Разные браузеры поддерживают разные форматы, поэтому часто указывают несколько <source>.




                                            CSS

                            3.6. Псевдоклассы (состояния)

        Описывают состояние элемента, часто — при взаимодействии:

        :hover — при наведении мыши.
        :active — в момент клика.
        :focus — при получении фокуса (например, поле ввода).
        :first-child — первый ребёнок родителя.
        :last-child — последний ребёнок.
        :nth-child(2) — n-ый ребёнок.


                        3.7. Псевдоэлементы
        Создают «виртуальные» элементы. Пишутся с двумя двоеточиями ::.

        ::before — перед содержимым.
        ::after — после содержимого.
        ::first-line — первая строка.
        ::selection — выделенный текст.


                Стиль и декорация
        em { font-style: italic; }        /* курсив */
        u { text-decoration: underline; } /* подчёркивание */
        del { text-decoration: line-through; } /* зачёркивание */
        a { text-decoration: none; }      /* убрать подчёркивание у ссылок */
        5.5. Выравнивание text-align
        p { text-align: left; }   /* по левому краю (по умолч.) */
        p { text-align: center; } /* по центру */
        p { text-align: right; }  /* по правому */
        p { text-align: justify; } /* по ширине (выравнивает оба края) */
        5.6. Межстрочный интервал line-height
        p {
            line-height: 1.5;      /* 1.5 от размера шрифта */
        }
        Удобно задавать без единиц — это число, умножаемое на размер шрифта.

        5.7. Регистр и пробелы
        h1 { text-transform: uppercase; }  /* ВЕРХНИЙ РЕГИСТР */
        h1 { text-transform: lowercase; }
        p { text-transform: capitalize; }  /* Каждое слово с большой */
        p { letter-spacing: 2px; }         /* расстояние между буквами */
        p { word-spacing: 4px; }           /* между словами */
        p { white-space: nowrap; }         /* запрет переносов */
        5.8. Тень текста text-shadow
        h1 {
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }
        Формат: сдвиг_по_X сдвиг_по_Y размытие цвет.

       
       Объявление и использование
        :root {
            --main-color: #3498db;
            --main-padding: 16px;
            --border-radius: 8px;
        }

        .button {
            background: var(--main-color);
            padding: var(--main-padding);
            border-radius: var(--border-radius);
        }

        Объявление: --имя: значение; (двойной дефис перед именем).
        Использование: var(--имя).


        Это основа тем оформления (светлая/тёмная тема):

        :root { --bg: #fff; --text: #222; }
        @media (prefers-color-scheme: dark) {
            :root { --bg: #222; --text: #eee; }
        }
        body { background: var(--bg); color: var(--text); }


             Сброс браузерных стилей форм

    Встроенные стили форм различаются между браузерами. Для начала зададим единый вид:

    input,
    textarea,
    select,
    button {
        font: inherit;            /* наследовать шрифт от страницы */
        box-sizing: border-box;   /* размеры с учётом padding/border */
        appearance: none;         /* убрать «нативную» внешность (осторожно с checkbox/radio) */
    }


    1.3. Псевдоклассы состояния формы

            Псевдокласс	                Значение
            :focus	            Поле в фокусе (активен, готов к вводу)
            :focus-visible	    Фокус, видимый при навигации с клавиатуры (⚠️ важно для a11y)
            :hover	            Наведение мыши
            :disabled	        Заблокированное поле
            :placeholder-shown	Поле, где плейсхолдер виден (пустое)
            :invalid	        Неверно заполнено
            :valid	            Заполнено верно
            :checked	        Отмеченный чекбокс/радио
            :required	        Обязательное поле

    input:required {
        border-left: 4px solid #f39c12;
    }
    input:invalid:not(:placeholder-shown) {
        border-color: #e74c3c;      /* ошибка, если введено и неверно */
    }
    input:valid {
        border-color: #27ae60;
    }





     Продвинутая типографика
    2.1. Подключение веб-шрифтов через @font-face
    @font-face подключает локальные файлы шрифтов:

    @font-face {
        font-family: "MyFont";
        src: url("myfont.woff2") format("woff2");
        font-weight: 400;
        font-display: swap; /* показать системный, пока грузится (лучший UX) */
    }
    @font-face {
        font-family: "MyFont";
        src: url("myfont-bold.woff2") format("woff2");
        font-weight: 700;
    }
    Затем:

    body { font-family: "MyFont", sans-serif; }
    h1 { font-weight: 700; }
    2.2. Google Fonts (быстрый способ)
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    body { font-family: 'Roboto', sans-serif; }




    Tрансформации и переходы
    1.1. transform — изменение геометрии
    transform поворачивает, масштабирует, сдвигает и наклоняет элемент. Элемент выпадает из потока визуально, но его «место» остаётся.

    .box {
        transform: rotate(45deg);          /* поворот на 45° */
        transform: scale(1.2);             /* увеличение в 1.2 раза */
        transform: translate(10px, 20px);  /* сдвиг по X, Y */
        transform: skew(10deg);            /* наклон */
    }
    Комбинирование (запятые не нужны, порядок важен):

    .box {
        transform: translate(-50%, -50%) rotate(10deg) scale(1.1);
    }
    1.2. transition — плавное изменение
    transition заставляет свойства меняться плавно, когда они изменяются (например, при :hover).

    .card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0,0,0,0.2);
    }
    Синтаксис: transition: свойство длительность [задержка] [timing-function].

    1.3. timing-function — функция скорости
    Контролирует, как меняется скорость во времени:

    Функция	Характер
    linear	Равномерно
    ease	Старт быстрый, замедление в конце (по умолч.)
    ease-in	Плавный старт, ускорение к концу
    ease-out	Быстрый старт, плавное торможение
    ease-in-out	Плавно и в начале, и в конце
    cubic-bezier(...)	Произвольная кривая
    steps(n)	Ступенчатая (для анимации кадров)
    .box { transition: all 0.4s cubic-bezier(0.68, -0.55, 0.27, 1.55); }
    /* отскок (overshoot): значение «вылетает» за пределы и возвращается */
    1.4. Практические hover-эффекты
    /* Подъём карточки */
    .card { transition: transform 0.2s ease; }
    .card:hover { transform: translateY(-4px); }

    /* Кнопка с «переливанием» */
    .btn { position: relative; overflow: hidden; }
    .btn::after {
        content: "";
        position: absolute;
        inset: 0;
        background: rgba(255,255,255,0.2);
        transform: translateX(-100%);
        transition: transform 0.4s ease;
    }
    .btn:hover::after { transform: translateX(0); }

             /* Плавное появление */
    .fade { opacity: 0; transition: opacity 0.5s; }
    .fade.visible { opacity: 1; }
    1.5. Переходы с transform и opacity — лучший выбор
    Браузер быстрее анимирует transform и opacity (они выносятся на GPU). Анимировать width, height, margin — дорого и дёргано. Предпочтительно:

                 /* Плохо */
    li { transition: height 0.3s; }
    /* Хорошо */
    .panel { transform: scaleY(0); transform-origin: top; transition: transform 0.3s; }
    .panel.open { transform: scaleY(1); }
    2. Анимации на @keyframes
    animation позволяет создавать многошаговые и самостоятельные анимации (в отличие от transition, который лишь реагирует на изменение).

                2.1. Определение keyframes
    @keyframes bounce {
        0%   { transform: translateY(0); }
        50%  { transform: translateY(-20px); }
        100% { transform: translateY(0); }
    }
     Можно писать только ключевые моменты from (0%) и to (100%):

    @keyframes fadeIn {
        from { opacity: 0; }
        to   { opacity: 1; }
    }



         2.2. Применение animation
    .ball {
        animation: bounce 1s infinite;
    }
    2.3. Все свойства animation
    Свойство	                            Описание
    animation-name	                    Имя keyframes
    animation-duration	                Длительность (например, 0.8s)
    animation-timing-function	        Кривая скорости
    animation-delay	                    Задержка старта
    animation-iteration-count	        Число повторов / infinite
    animation-direction	                normal, reverse, alternate, alternate-reverse
    animation-fill-mode	Что до/после:   none, forwards, backwards, both
    animation-play-state	            running / paused
    Сокращённая запись:

    .el {
        animation: name duration timing-func delay iteration direction fill-mode;
    }
    .ball {
        animation: bounce 1s ease-in-out 0.2s infinite alternate;
    }


            2.4. fill-mode — состояние до и после
    forwards — сохранить конечное состояние после завершения.
    backwards — применить начальное состояние до старта (с учётом задержки).
    both — и то, и другое (удобнее всего).
    .box { animation: fadeIn 1s both; }
    both часто ставят, чтобы анимация не «прыгала» в начале/конце.

            2.5. Управление через JS / state (hover)
    .spinner {
        animation: spin 1s linear infinite;
    }
    .spinner.paused {
        animation-play-state: paused; /* можно переключать через класс в JS */
    }
    @keyframes spin {
        to { transform: rotate(360deg); }
    }


             2.6. Примеры анимаций
    Пульсация кнопки:

    @keyframes pulse {
        0% { box-shadow: 0 0 0 0 rgba(52,152,219,0.7); }
        70% { box-shadow: 0 0 0 15px rgba(52,152,219,0); }
        100% { box-shadow: 0 0 0 0 rgba(52,152,219,0); }
    }
    .btn { animation: pulse 2s infinite; }
    Покачивание:

    @keyframes wiggle {
        0%, 100% { transform: rotate(-3deg); }
        50% { transform: rotate(3deg); }
    }
    .icon { animation: wiggle 1.2s ease-in-out infinite; }


             2.7. prefers-reduced-motion (доступность!)
    Уважайте пользователей, отключивших анимации в системе:

    @media (prefers-reduced-motion: reduce) {
        *, *::before, *::after {
            animation-duration: 0.01ms !important;
            animation-iteration-count: 1 !important;
            transition-duration: 0.01ms !important;
        }
    }
